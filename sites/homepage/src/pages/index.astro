---
import Base from "../layouts/Base.astro";
import { EmbeddedApp } from "../components/EmbeddedApp";
---

<Base>
  <!--
    The app IS the page. On first visit the Help modal opens to the
    About tab (hero content, download/GitHub links, feature list).
    On subsequent visits it loads straight into the deck-builder.
  -->
  <EmbeddedApp client:only="react" />
</Base>
